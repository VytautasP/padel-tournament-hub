# Padel Tournament Hub

A PWA for running padel Americano / Mixicano / Team Americano sessions.
See `README.md` for the stack and `docs/DECISIONS.md` for locked design decisions.

## Agent skills

### Issue tracker

Issues live in GitHub Issues on `VytautasP/padel-tournament-hub`, driven by the `gh` CLI.
See `docs/agents/issue-tracker.md`.

### Triage labels

Canonical label names, used verbatim — no mapping.
See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the root, with `docs/DECISIONS.md` as the frozen
design record. See `docs/agents/domain.md`.
